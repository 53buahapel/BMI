# BMI (Body Mass Index) Calculator

requirement:

- nodejs
- mysql

## How to set

1. clone this repository
2. run `npm install` within folder
3. create databases in mysql
4. create `.env` file in root folder
5. to start server run `npm start` within folder
